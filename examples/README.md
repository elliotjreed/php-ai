# Examples

Runnable examples showing plain-text prompts, structures prompts, and images using image URLs and base64 encoding file contents are provided.

You'll need to provide your own API keys, from OpenAI for ChatGPT models, and Anthropic for Claude models.

To run the examples, in the root of this project run:

```bash
composer install
```
